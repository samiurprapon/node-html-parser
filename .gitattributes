# Normalize line endings to LF for all text files.
# Critical for a cross-platform npm package (avoids CRLF in published .js).
# Git auto-detects binary files and leaves them untouched.
* text=auto eol=lf

# Windows forced line-endings
/.idea/* text eol=crlf
